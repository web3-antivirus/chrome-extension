import React from 'react';
import ReactDOM from 'react-dom';
import 'assets/scss/global.scss';
import PhishingPage from 'containers/PhishingPage';

ReactDOM.render(
  <React.StrictMode>
    <PhishingPage />
  </React.StrictMode>,
  document.getElementById('root'),
);
